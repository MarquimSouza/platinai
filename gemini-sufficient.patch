diff --git a/src/app/api/guides/generate/route.ts b/src/app/api/guides/generate/route.ts
index d5e96df..097bb79 100644
--- a/src/app/api/guides/generate/route.ts
+++ b/src/app/api/guides/generate/route.ts
@@ -53,18 +53,50 @@ export async function POST(req: NextRequest) {
 
     const videoQuery = `${gameName} ${achievementName} achievement`
 
-    const [searchContext, videoUrl] = await Promise.all([
+    const [searchResult, videoUrl] = await Promise.all([
       searchAchievementGuide(searchQuery),
       searchYoutubeVideo(videoQuery, achievementName),
     ])
 
-    const { pt, en } = await generateAchievementGuide({
+    if (!searchResult.hasEnoughContent) {
+      // Conta a busca no rate limit (Tavily/YouTube já foram consultados),
+      // mas NÃO salva no Supabase — assim o usuário pode tentar de novo depois,
+      // sem depender do botão de regenerar (que não existe mais).
+      await supabaseAdmin.from("generation_log").insert({ ip })
+
+      return NextResponse.json({
+        guideTextPt:
+          "Não encontramos conteúdo suficiente na web para gerar uma dica confiável para esta conquista ainda. Tente novamente mais tarde — pode surgir conteúdo novo.",
+        guideTextEn:
+          "We couldn't find enough web content to generate a reliable hint for this achievement yet. Try again later — new content may show up.",
+        videoUrl,
+        cached: false,
+        insufficientContent: true,
+      })
+    }
+
+    const { pt, en, sufficient } = await generateAchievementGuide({
       gameName,
       achievementName,
       achievementDescription: description,
-      searchContext,
+      searchContext: searchResult.text,
     })
 
+    // Mesmo com texto suficiente em tamanho (filtro do Tavily), o próprio Gemini pode achar
+    // o conteúdo genérico/insuficiente para dar passos práticos — nesse caso, tratamos igual
+    // ao caso de "sem conteúdo": não salva no cache, mostra como aviso.
+    if (!sufficient) {
+      await supabaseAdmin.from("generation_log").insert({ ip })
+
+      return NextResponse.json({
+        guideTextPt: pt,
+        guideTextEn: en,
+        videoUrl,
+        cached: false,
+        insufficientContent: true,
+      })
+    }
+
     await supabaseAdmin.from("achievement_guides").insert({
       appid: String(appid),
       apiname,
diff --git a/src/lib/gemini.ts b/src/lib/gemini.ts
index 242a489..3f16de6 100644
--- a/src/lib/gemini.ts
+++ b/src/lib/gemini.ts
@@ -8,6 +8,7 @@ type GuideRequest = {
 type GuideResult = {
   pt: string
   en: string
+  sufficient: boolean
 }
 
 export async function generateAchievementGuide({
@@ -29,10 +30,12 @@ Descrição oficial: ${achievementDescription}
 ${searchContext}
 --- FIM DO CONTEÚDO ---
 
-Escreva um guia curto e prático (no máximo 5-6 frases, direto ao ponto, com passos práticos) em DUAS versões: uma em português e uma em inglês, com o MESMO conteúdo nas duas, apenas traduzido. Se o conteúdo pesquisado não for suficiente pra responder com confiança, diga isso claramente nas duas versões, em vez de inventar.
+Escreva um guia curto e prático (no máximo 5-6 frases, direto ao ponto, com passos práticos) em DUAS versões: uma em português e uma em inglês, com o MESMO conteúdo nas duas, apenas traduzido.
+
+Além do texto, avalie você mesmo se o conteúdo pesquisado foi suficiente para dar passos práticos e específicos (não apenas título/dados genéricos do jogo). Se não foi suficiente, ainda assim escreva nas duas versões uma frase curta dizendo isso, e marque "sufficient": false.
 
 Responda ESTRITAMENTE em JSON válido, sem markdown, sem \`\`\`, no formato exato:
-{"pt": "texto em português aqui", "en": "text in english here"}`
+{"pt": "texto em português aqui", "en": "text in english here", "sufficient": true ou false}`
 
   const res = await fetch(
     `https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent`,
@@ -64,12 +67,15 @@ Responda ESTRITAMENTE em JSON válido, sem markdown, sem \`\`\`, no formato exat
     return {
       pt: parsed.pt ?? "Não foi possível gerar um guia para esta conquista.",
       en: parsed.en ?? "Could not generate a guide for this achievement.",
+      // Se o Gemini não mandar o campo por algum motivo, assume true (comportamento anterior à essa mudança)
+      sufficient: parsed.sufficient ?? true,
     }
   } catch {
     // Se o Gemini não devolver JSON limpo por algum motivo, usa o texto cru como fallback nas duas versões
     return {
       pt: rawText || "Não foi possível gerar um guia para esta conquista.",
       en: rawText || "Could not generate a guide for this achievement.",
+      sufficient: true,
     }
   }
 }
\ No newline at end of file
